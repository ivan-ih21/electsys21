% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-utilities.R
\name{gen_utilities}
\alias{gen_utilities}
\title{Generate random utility ballots}
\usage{
gen_utilities(
  n_voters,
  n_candidates,
  scale = NULL,
  p_abstain = 0,
  candidate_names = NULL,
  voter_names = NULL,
  seed = NULL
)
}
\arguments{
\item{n_voters}{Single positive integer. Number of voters (matrix rows).}

\item{n_candidates}{Single positive integer. Number of candidates (matrix
columns).}

\item{scale}{\code{NULL} or a numeric vector \code{c(lo, hi)} with \code{lo < hi} giving
the range of generated utilities. Defaults to \code{NULL} (the unit interval
\code{c(0, 1)}).}

\item{p_abstain}{Single number in \verb{[0, 1]}. Probability that a voter
abstains on any given candidate, leaving that entry \code{NA}. Defaults to \code{0}.}

\item{candidate_names}{\code{NULL} or a character vector of length
\code{n_candidates} giving the column names. Defaults to \code{NULL}
(\code{Candidate_1}, \code{Candidate_2}, ...).}

\item{voter_names}{\code{NULL} or a character vector of length \code{n_voters} giving
the row names. Defaults to \code{NULL} (\code{Voter_1}, \code{Voter_2}, ...).}

\item{seed}{\code{NULL} or a single numeric value passed to \code{\link[=set.seed]{set.seed()}} for
reproducible output. Defaults to \code{NULL}.}
}
\value{
A numeric matrix with \code{n_voters} rows and \code{n_candidates} columns of
utilities (higher = more preferred); abstained entries are \code{NA}. Suitable
as the \code{x} argument of the voting functions.
}
\description{
Simulates a cardinal utility-ballot matrix for testing and demonstration.
Utilities are drawn uniformly at random over a configurable scale (higher =
more preferred), and voters may abstain on some or all candidates.
}
\examples{
gen_utilities(n_voters = 5, n_candidates = 3, seed = 1)

# Utilities on a 0-10 scale with occasional abstentions
gen_utilities(n_voters = 5, n_candidates = 3,
              scale = c(0, 10), p_abstain = 0.1, seed = 1)

}
\seealso{
\code{\link[=gen_ranks]{gen_ranks()}}, \code{\link[=gen_approvals]{gen_approvals()}}
}
