% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condorcet.R
\name{condorcet}
\alias{condorcet}
\title{Condorcet Method}
\usage{
condorcet(
  x,
  type = c("auto", "rank", "utility", "approval"),
  ties = c("random", "lexicographic", "all"),
  return_pairwise = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_pairwise}{Logical. If \code{TRUE}, two extra components are attached
to the result: \verb{$pairwise}, the candidate-by-candidate matrix where
\verb{[i, j]} is \code{1} iff candidate \code{i} beats candidate \code{j} head-to-head, and
\verb{$pairwise_margins}, the matrix whose \verb{[i, j]} entry counts the voters who
prefer candidate \code{i} over candidate \code{j}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"condorcet_result"}, a list with elements
\code{summary} (a data frame of candidates ordered by Copeland score, with
columns \code{candidate}, \code{wins}, \code{losses}, \code{ties}, \code{rank} and \code{status}),
\code{winners} (name(s) of the Condorcet winner, or \code{character(0)} if none),
\code{losers} (name(s) of the Condorcet loser, or \code{character(0)} if none),
\code{n_voters}, \code{n_valid}, \code{n_candidates}, and \code{method} (a list recording the
inferred \code{type} and the \code{ties} rule). When \code{return_pairwise = TRUE} the
elements \code{pairwise} and \code{pairwise_margins} are also included. Print the
object or call \code{\link[=summary]{summary()}} on it for a formatted results table.
}
\description{
Determines the winner by comparing every candidate head-to-head against
every other candidate. In each pairwise contest the candidate preferred by a
strict majority of voters wins; the \strong{Condorcet winner} is the candidate who
beats all others. Such a candidate need not exist, in which case \code{winners} is
empty. A Copeland-style score (wins minus losses) is always computed so the
full field is ordered even when no Condorcet winner exists.
}
\examples{
ballots <- gen_ranks(n_voters = 30, n_candidates = 4, seed = 1)
condorcet(ballots)

condorcet(ballots, return_pairwise = TRUE)

}
\seealso{
\code{\link[=borda]{borda()}}, \code{\link[=irv]{irv()}}, \code{\link[=approval]{approval()}}
}
