% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen-approvals.R
\name{gen_approvals}
\alias{gen_approvals}
\title{Generate random approval ballots}
\usage{
gen_approvals(
  n_voters,
  n_candidates,
  p_approve = 0.5,
  p_abstain = 0,
  candidate_names = NULL,
  voter_names = NULL,
  seed = NULL
)
}
\arguments{
\item{n_voters}{Single positive integer. Number of voters (matrix rows).}

\item{n_candidates}{Single positive integer. Number of candidates (matrix
columns).}

\item{p_approve}{Single number in \verb{[0, 1]}. Probability that a voter
approves any given candidate. Defaults to \code{0.5}.}

\item{p_abstain}{Single number in \verb{[0, 1]}. Probability that a voter
abstains entirely, leaving that whole row \code{NA}. Defaults to \code{0}.}

\item{candidate_names}{\code{NULL} or a character vector of length
\code{n_candidates} giving the column names. Defaults to \code{NULL}
(\code{Candidate_1}, \code{Candidate_2}, ...).}

\item{voter_names}{\code{NULL} or a character vector of length \code{n_voters} giving
the row names. Defaults to \code{NULL} (\code{Voter_1}, \code{Voter_2}, ...).}

\item{seed}{\code{NULL} or a single numeric value passed to \code{\link[=set.seed]{set.seed()}} for
reproducible output. Defaults to \code{NULL}.}
}
\value{
A logical matrix with \code{n_voters} rows and \code{n_candidates} columns:
\code{TRUE} marks an approved candidate, \code{FALSE} a non-approved one, and an
abstaining voter's row is all \code{NA}. Suitable as the \code{x} argument of the
voting functions.
}
\description{
Simulates an approval-ballot matrix for testing and demonstration. Each
candidate is independently approved by each voter with probability
\code{p_approve}, and voters may abstain entirely.
}
\examples{
gen_approvals(n_voters = 5, n_candidates = 3, seed = 1)

# Stricter voters approve fewer candidates
gen_approvals(n_voters = 5, n_candidates = 3, p_approve = 0.25, seed = 1)

}
\seealso{
\code{\link[=gen_ranks]{gen_ranks()}}, \code{\link[=gen_utilities]{gen_utilities()}}
}
