% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fptp.R
\name{fptp}
\alias{fptp}
\title{First-Past-The-Post voting}
\usage{
fptp(
  x,
  type = c("auto", "rank", "utility", "approval"),
  ties = c("random", "lexicographic", "all"),
  return_ranks = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_ranks}{Logical. If \code{TRUE}, the ranking matrix derived
internally is attached to the result as \verb{$ranks}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"fptp_result"}, a list with elements:
\itemize{
\item \code{summary}: a data frame of candidates ordered by votes, with columns
\code{candidate}, \code{vote}, \code{percentage} (share of valid votes) and \code{rank}.
\item \code{winners}: a character vector of the winning candidate name(s).
\item \code{n_voters}: the total number of voters (rows of \code{x}).
\item \code{n_candidates}: the total number of candidates (columns of \code{x}).
\item \code{method}: a list recording the inferred \code{type} and the \code{ties} rule used.
\item \code{ranks}: optionally, the derived ranking matrix, present only when
\code{return_ranks = TRUE}.
}

Print the object or call \code{\link[=summary]{summary()}} on it for a formatted results table.
}
\description{
Runs a First-Past-The-Post election. Each voter contributes a single
first-preference vote, and the candidate receiving the most first-preference
votes wins. The function accepts ranking, utility (score) or approval
ballots, reduces each ballot to its single top choice, tallies the
first-preference votes, and resolves any tie for the lead according to
\code{ties}.
}
\examples{
ballots <- gen_ranks(n_voters = 20, n_candidates = 4, seed = 1)
fptp(ballots)

fptp(ballots, ties = "lexicographic", return_ranks = TRUE)

}
\seealso{
\code{\link[=borda]{borda()}}, \code{\link[=irv]{irv()}}, \code{\link[=condorcet]{condorcet()}}
}
