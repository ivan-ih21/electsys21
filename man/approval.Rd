% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/approval.R
\name{approval}
\alias{approval}
\title{Approval Voting}
\usage{
approval(
  x,
  type = c("auto", "rank", "utility", "approval"),
  rule = c("mean", "median", "topk", "topp", "quantile", "above0", "nonzero"),
  threshold = NULL,
  ties = c("random", "lexicographic", "all"),
  return_approvals = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{rule}{The approval rule used to convert non-approval ballots (ranks or
utilities) into approvals; ignored when the input is already an approval
matrix. May be a preset string -- \code{"mean"} (the default; score
above the voter's mean), \code{"median"} (above the voter's median), \code{"topk"}
(top \code{k} by score, \code{k} from \code{threshold}), \code{"topp"} (top proportion by
score, \code{p} from \code{threshold}), \code{"quantile"} (above the \code{threshold}-th
quantile), \code{"above0"} (positive score, or all ranked candidates for rank
input), or \code{"nonzero"} (non-zero score, or all ranked candidates for rank
input) -- or a numeric scalar/vector of length \code{ncol(x)} (eligible if
\code{score > rule} for utility, or \code{rank <= rule} for rank input), or a custom
function with signature \verb{function(row, inferred_type)} returning a logical
vector of length \code{ncol(x)}.}

\item{threshold}{Single numeric value (or \code{NULL}, the default) parameterizing
the \code{"topk"} (number of candidates to approve), \code{"topp"} (proportion in
\verb{[0, 1]}) and \code{"quantile"} (quantile in \verb{[0, 1]}) presets. Ignored for all
other rules.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_approvals}{Logical; if \code{TRUE}, the result additionally includes
the derived binary approval matrix as \verb{$approvals}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"approval_result"}, a list with elements
\code{summary} (a data frame of candidates, approval counts, percentages and
ranks), \code{winners} (the winning candidate name(s)), \code{n_voters}, \code{n_valid},
\code{n_candidates} and \code{method} (a list recording the inferred \code{type}, \code{rule},
\code{ties} and \code{threshold}). When \code{return_approvals = TRUE}, the derived binary
approval matrix is also returned as \verb{$approvals}. Print the object or call
\code{\link[=summary]{summary()}} on it for a formatted results table.
}
\description{
Runs an election under Approval Voting, where each voter may approve any
number of candidates and the candidate with the most approvals wins. When
the input is not already a binary approval matrix, \code{rule} converts the
rankings or utility scores into approvals. Approvals are then summed across
voters and ties for the top approval count are resolved according to \code{ties}.
}
\examples{
# Direct approval ballots
ballots <- gen_approvals(n_voters = 50, n_candidates = 4, seed = 1)
approval(ballots)

# Utility ballots converted to approvals via a rule
scores <- gen_utilities(n_voters = 50, n_candidates = 4, seed = 1)
approval(scores, type = "utility", rule = "mean")

}
\seealso{
\code{\link[=fptp]{fptp()}}, \code{\link[=borda]{borda()}}, \code{\link[=tworound]{tworound()}}
}
