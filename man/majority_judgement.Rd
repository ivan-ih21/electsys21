% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/majority_judgement.R
\name{majority_judgement}
\alias{majority_judgement}
\title{Majority Judgment}
\usage{
majority_judgement(
  x,
  type = c("auto", "rank", "utility", "approval", "score"),
  num_categories = NULL,
  median_tie_break = c("gauge", "iterative"),
  ties = c("random", "lexicographic", "all"),
  return_grades = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"}, \code{"approval"} or
\code{"score"}. The \code{"score"} type treats each entry directly as an integer
grade.}

\item{num_categories}{Integer \verb{>= 2} giving the number of grade levels \code{K},
or \code{NULL} (the default) to choose a sensible value from the inferred type:
\code{2} for approval, the number of candidates for rank, \code{min(5, max(2, n_candidates))} for utility, and the maximum observed grade for score.
Approval is always coerced to \code{2}, rank is capped at the number of
candidates (both with a warning), and a score \code{K} below the maximum
observed grade is an error.}

\item{median_tie_break}{Character string giving the rule used to separate
candidates that share the same median grade. One of \code{"gauge"} (the default;
compares the majority-gauge proportions of voters above and below the
median) or \code{"iterative"} (compares the majority sequences obtained by
repeatedly removing the median grade).}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_grades}{Logical. If \code{TRUE}, the internal integer grade matrix
(voters by candidates, on the \verb{1..K} scale) is attached to the result as
\verb{$grades}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"majority_judgement_result"}: a list with
elements \code{summary} (a data frame with columns \code{candidate}, \code{median},
\code{p_above}, \code{p_below} and \code{rank}), \code{winners}, \code{n_voters}, \code{n_valid},
\code{n_candidates}, \code{grade_distribution} and \code{method} (itself a list of \code{type},
\code{num_categories}, \code{median_tie_break} and \code{ties}), plus \code{grades} when
\code{return_grades = TRUE}. Print the object or call \code{\link[=summary]{summary()}} on it for a
formatted results table.
}
\description{
Runs the Majority Judgment voting. Each voter assigns every candidate an
absolute grade on a common ordinal scale (\code{1} = worst, \code{K} = best), and for
each candidate the \strong{median grade} across voters is computed (using the lower
of the two middle grades when the count is even). The candidate with the
highest median wins. Ties on the median are resolved either by the
majority-gauge proportions or by the iterative "majority sequence" rule, which
repeatedly removes the median grade until the candidates' sequences diverge.
}
\examples{
utilities <- gen_utilities(n_voters = 30, n_candidates = 4, seed = 1)
majority_judgement(utilities)

majority_judgement(utilities, num_categories = 5, median_tie_break = "iterative")

}
\seealso{
\code{\link[=borda]{borda()}}, \code{\link[=approval]{approval()}}, \code{\link[=condorcet]{condorcet()}}
}
