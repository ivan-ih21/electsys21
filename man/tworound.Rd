% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tworound.R
\name{tworound}
\alias{tworound}
\title{Two-Round System (runoff) election}
\usage{
tworound(
  x,
  type = c("auto", "rank", "utility", "approval"),
  ties = c("random", "lexicographic", "all"),
  return_ranks = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_ranks}{Logical. If \code{TRUE}, the ranking matrix derived
internally is attached to the result as \verb{$ranks}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"tworound_result"}, a list with elements:
\code{round1} (a list with the first-round \code{summary} data frame and \code{n_valid}
vote count), \code{round2} (a list with the second-round \code{summary} and
\code{n_valid}, or \code{NULL} when the election is decided in round 1),
\code{finalists} (the candidate name(s) advancing to round 2, or
\code{character(0)}), \code{winners} (the winning candidate name(s)), \code{n_voters},
\code{n_candidates}, and \code{method} (a list recording \code{type}, \code{ties}, and
\code{tworound}, a logical flag for whether a second round was held).
If \code{return_ranks = TRUE}, the derived ranking matrix is also attached as
\verb{$ranks}. Print the object or call \code{\link[=summary]{summary()}} on it for a formatted
results table.
}
\description{
Runs a Two-Round System election. In the first round each voter's top
preference is counted; if a candidate wins more than 50\% of the valid votes
they are elected outright. Otherwise the top two candidates advance to a
second round, where each voter's preference between the two finalists is
counted and the finalist with the most votes wins. The majority threshold is
strictly greater than 50\%.
}
\examples{
ranks <- gen_ranks(n_voters = 30, n_candidates = 4, seed = 1)
tworound(ranks)

utilities <- gen_utilities(n_voters = 30, n_candidates = 4, seed = 1)
tworound(utilities, type = "utility", ties = "lexicographic")

}
\seealso{
\code{\link[=fptp]{fptp()}}, \code{\link[=irv]{irv()}}, \code{\link[=borda]{borda()}}
}
