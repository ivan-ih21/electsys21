% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irv.R
\name{irv}
\alias{irv}
\title{Instant Runoff Voting}
\usage{
irv(
  x,
  type = c("auto", "rank", "utility", "approval"),
  ties = c("random", "lexicographic", "all"),
  elim_ties = c("random", "lexicographic", "all"),
  majority = 0.5,
  return_history = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{elim_ties}{Character string giving the tie-breaking rule applied at the
elimination step when two or more active candidates share the lowest vote
count in a round. One of \code{"random"} (the default; eliminate one tied
candidate at random), \code{"lexicographic"} (eliminate the alphabetically
first) or \code{"all"} (batch elimination: simultaneously remove every candidate
tied at the minimum).}

\item{majority}{Single numeric value in \verb{[0, 1]} giving the fraction of
active votes a candidate must \strong{strictly exceed} to win a round. Defaults
to \code{0.5}, so a candidate needs more than 50\% of the active votes.}

\item{return_history}{Logical. If \code{TRUE}, the result gains a \verb{$history}
element: a character matrix (\code{n_voters} x \code{n_rounds}) recording which active
candidate received each voter's vote in each round, or \code{NA} where the
ballot was exhausted. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"irv_result"}, a list with the elements:
\itemize{
\item \code{summary}: a data frame with columns \code{candidate}, \code{vote}, \code{percentage},
\code{rank} and \code{eliminated_in_round}, where \code{vote} and \code{percentage} reflect
each candidate's score at their last appearance.
\item \code{winners}: character vector of the winning candidate name(s).
\item \code{n_voters}: total number of voters.
\item \code{n_valid}: number of voters with at least one valid preference.
\item \code{n_candidates}: total number of candidates.
\item \code{rounds}: a list with one entry per round (active set, vote counts,
percentages, totals, exhausted ballots and candidate(s) eliminated).
\item \code{counts}: candidate-by-round matrix of integer vote counts.
\item \code{percentages}: candidate-by-round matrix of active-vote percentages.
\item \code{method}: a list recording \code{type}, \code{ties}, \code{elim_ties} and \code{majority}.
\item \code{history}: present only when \code{return_history = TRUE} (see that argument).
}

Print the object or call \code{\link[=summary]{summary()}} on it for a formatted results table.
}
\description{
Runs an Instant Runoff Voting. Each voter ranks the candidates, and in each
round the candidate with the fewest first-preference votes among the active
candidates is eliminated and their ballots transfer to the next-ranked active
candidate. The process repeats until a candidate's share of the active
(non-exhausted) votes \strong{strictly exceeds} the \code{majority} threshold or only
one candidate remains.
}
\examples{
ballots <- gen_ranks(n_voters = 40, n_candidates = 4, seed = 1)
result <- irv(ballots)
result

# Deterministic tie-breaking and round-by-round history
irv(ballots, ties = "lexicographic", elim_ties = "lexicographic",
    return_history = TRUE)

}
\seealso{
\code{\link[=fptp]{fptp()}}, \code{\link[=tworound]{tworound()}}, \code{\link[=borda]{borda()}}
}
