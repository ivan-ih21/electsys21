% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/d21_minus.R
\name{d21_minus}
\alias{d21_minus}
\title{D21 Voting with minus votes}
\usage{
d21_minus(
  x,
  type = c("auto", "rank", "utility", "approval"),
  rule = c("mean", "median", "topk", "topp", "quantile", "above0", "nonzero"),
  threshold = NULL,
  minus_prob = 1,
  ties = c("random", "lexicographic", "all"),
  overflow = c("random", "lexicographic"),
  return_approvals = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{rule}{The approval rule used to convert non-approval ballots (ranks or
utilities) into eligibility for plus votes; ignored when the input is
already an approval matrix. May be a preset string -- \code{"mean"} (the
default; score above the voter's mean), \code{"median"} (above the voter's
median), \code{"topk"} (top \code{k} by score, \code{k} from \code{threshold}), \code{"topp"} (top
proportion by score, \code{p} from \code{threshold}), \code{"quantile"} (above the
\code{threshold}-th quantile), \code{"above0"} (positive score, or all ranked
candidates for rank input), or \code{"nonzero"} (non-zero score, or all ranked
candidates for rank input) -- or a numeric scalar/vector of length
\code{ncol(x)} (eligible if \code{score > rule} for utility, or \code{rank <= rule} for
rank input), or a custom function with signature \verb{function(row, inferred_type)}
returning a logical vector of length \code{ncol(x)}.}

\item{threshold}{Single numeric value (or \code{NULL}, the default) parameterizing
the \code{"topk"} (number of candidates to approve), \code{"topp"} (proportion in
\verb{[0, 1]}) and \code{"quantile"} (quantile in \verb{[0, 1]}) presets. Ignored for all
other rules.}

\item{minus_prob}{Numeric in \verb{[0, 1]} (default \code{1}) giving the probability
that a voter eligible for a minus vote actually casts it. Setting
\code{minus_prob = 0} suppresses all minus votes.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{overflow}{Character string giving the tie-breaking rule used at the
plus-vote cap, within the \code{"topk"}/\code{"topp"} rules, and when selecting the
worst candidate for the minus vote. One of \code{"random"} (the default; sample
among tied candidates) or \code{"lexicographic"} (choose alphabetically by name).}

\item{return_approvals}{Logical. If \code{TRUE}, the result additionally includes
the per-voter \verb{$plus_matrix}, \verb{$minus_matrix} and \verb{$total_matrix}. Defaults
to \code{FALSE}.}
}
\value{
An object of class \code{"d21_minus_result"}: a list with elements
\code{summary} (a data frame of per-candidate plus, minus and total counts with
percentages and ranks), \code{winners}, \code{n_voters}, \code{n_valid}, \code{n_candidates}
and \code{method} (a list recording \code{type}, \code{rule}, \code{ties}, \code{threshold},
\code{plus_votes}, \code{minus_votes}, \code{minus_prob} and \code{overflow}). When
\code{return_approvals = TRUE}, the elements \code{plus_matrix}, \code{minus_matrix} and
\code{total_matrix} are also included. Print the object or call \code{\link[=summary]{summary()}} on
it for a formatted results table.
}
\description{
Runs the single-winner D21 method with minus votes. Each voter receives a
fixed number of plus votes (1, 2, or 3, depending on the number of
candidates) to support their most preferred candidates, and voters who use at
least two plus votes may additionally cast one minus vote against the
candidate they most oppose. Eligibility for plus votes is derived from ranks
or utility scores via a configurable \code{rule} and then capped to the allowed
number; the minus vote targets the lowest-scored candidate the voter did not
plus. The candidate with the highest net total wins.
}
\examples{
# Utility ballots with 5 candidates (2 plus votes, 1 minus vote)
u <- gen_utilities(n_voters = 50, n_candidates = 5, seed = 1)
d21_minus(u)

# Ranking ballots, eligible = explicitly ranked candidates
r <- gen_ranks(n_voters = 50, n_candidates = 6, seed = 1)
d21_minus(r, rule = "above0", ties = "lexicographic")

# Suppress the minus phase (equivalent to plain D21)
d21_minus(u, minus_prob = 0)

}
\seealso{
\code{\link[=d21]{d21()}}, \code{\link[=approval]{approval()}}, \code{\link[=borda]{borda()}}
}
