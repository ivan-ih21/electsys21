% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/borda.R
\name{borda}
\alias{borda}
\title{Borda Count}
\usage{
borda(
  x,
  type = c("auto", "rank", "utility", "approval"),
  ties = c("random", "lexicographic", "all"),
  return_ranks = FALSE,
  return_scores = FALSE
)
}
\arguments{
\item{x}{A matrix of ballots with one row per voter and one column per
candidate. Accepts a ranking matrix (rank \code{1} = most preferred), a utility
(score) matrix (higher = more preferred), or a binary/logical approval
matrix. Column names are used as candidate names; if absent, candidates are
named \code{Candidate_1}, \code{Candidate_2}, and so on.}

\item{type}{Character string giving the ballot type. One of \code{"auto"} (the
default, detected from \code{x}), \code{"rank"}, \code{"utility"} or \code{"approval"}.}

\item{ties}{Character string giving the tie-breaking rule applied when
several candidates are tied for a winning position. One of \code{"random"} (the
default; pick one tied candidate at random), \code{"lexicographic"} (pick the
alphabetically first) or \code{"all"} (return all tied candidates).}

\item{return_ranks}{Logical. If \code{TRUE}, the ranking matrix derived
internally is attached to the result as \verb{$ranks}. Defaults to \code{FALSE}.}

\item{return_scores}{Logical. If \code{TRUE}, the per-voter Borda points matrix
(\code{n_voters} by \code{n_candidates}) used for scoring is attached to the result
as \verb{$scores}. Defaults to \code{FALSE}.}
}
\value{
An object of class \code{"borda_result"}, a list with elements:
\code{summary} (a data frame of candidates ordered by total score, with columns
\code{candidate}, \code{score}, \code{percentage} and \code{rank}), \code{winners} (a character
vector of the winning candidate name(s), or \code{character(0)} if no voter
provided a valid preference), \code{n_voters}, \code{n_valid}, \code{n_candidates}, and
\code{method} (a list recording the inferred \code{type} and the \code{ties} rule).
Optionally includes \verb{$ranks} (when \code{return_ranks = TRUE}) and \verb{$scores}
(when \code{return_scores = TRUE}). Print the object or call \code{\link[=summary]{summary()}} on it
for a formatted results table.
}
\description{
Runs an election using the Borda Count. Each voter's ballot is turned into
a ranking and each candidate earns points according to their position: with
\code{n} candidates, a candidate ranked first receives \code{n - 1} points, the second
\code{n - 2}, down to \code{0} for the last. Points are summed across all voters and
the candidate with the highest total wins. Utility and approval ballots are
converted to rankings internally before scoring.
}
\examples{
ballots <- gen_ranks(n_voters = 50, n_candidates = 4, seed = 1)
borda(ballots)

# Resolve ties alphabetically and keep the derived ranking matrix
borda(ballots, ties = "lexicographic", return_ranks = TRUE)

}
\seealso{
\code{\link[=fptp]{fptp()}}, \code{\link[=condorcet]{condorcet()}}, \code{\link[=approval]{approval()}}
}
